# Expose per-object (per-queue) metrics on the aggregated endpoint as well.
# The DLQDepth alert reads rabbitmq_queue_messages{queue="...dlq"} which is a
# per-object series (also available at /metrics/per-object, which Prometheus scrapes).
prometheus.return_per_object_metrics = true
